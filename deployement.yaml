apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: profileapp
  name: profileapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: profileapp
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: profileapp
    spec:
      containers:
      - image: afnan39/profile-app:v1.0.31
        name: profileapp
        resources: {}
status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: lb-service
  labels:
    app: lb-service
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: profileapp
